VITE_APP_TITLE=Tennis Diary Admin
VITE_APP_VERSION=1.0.0
VITE_API_BASE_URL=http://localhost:8000

# 构建期前端资源 base 路径（非 VITE_ 前缀，仅构建期读取，不注入产物）
# - 默认不设或留空 => 根路径 /（含 Cloudflare Workers 部署）
# - Nginx 挂 /admin/ 子路径 => BUILD_BASE=/admin/（建议用 pnpm build:nginx，脚本已内置）
# BUILD_BASE=/admin/
