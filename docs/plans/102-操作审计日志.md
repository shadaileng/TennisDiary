> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 102 |
> | 文档版本 | v1.6.1 |
> | 文档状态 | ✅ 已完成 |
> | 最后更新 | 2026-08-21 |
> | 对应功能/内容 | 操作审计日志（Admin + 小程序端全量写操作） |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-08-21 | v1.0.0 | 初版（路由函数 try/finally 方案） |
> | 2026-08-21 | v1.1.0 | 架构调整为中间件 + 装饰器方案 |
> | 2026-08-21 | v1.2.0 | 完成全部实现：后端 13 测试通过 + Admin 前端页面 + lint/build 通过 |
> | 2026-08-21 | v1.3.0 | 登录纳入审计（POST /api/auth/login 加 @audit 装饰器，端点总数 40→41） |
> | 2026-08-21 | v1.4.0 | created_at 时区修复：DateTime/func.now() → String(32)/手动 UTC Z 后缀，消除服务器时区依赖 |
> | 2026-08-21 | v1.5.0 | 装饰器 bug 修复：动态注入 request 参数到 wrapper 签名 + 中间件 fallback 从路由函数属性读取元数据 |
> | 2026-08-21 | v1.6.0 | 审计日志增加 request_path（路由路径）+ request_method（HTTP 方法），便于定位操作来源 |
> | 2026-08-21 | v1.6.1 | 查询端点漏传 request_path/request_method + 审计详情弹窗响应信息始终显示并移至请求体下方 |
>
> **关联文档**：[B2-1：管理员模型与认证](./44-B2-1-管理员模型与认证.md)、[72：Admin 备份管理增强](./72-Admin-备份管理增强.md)

# 操作审计日志

为所有写操作（Admin 端 + 小程序端）添加结构化审计日志，记录操作时间、发起人、IP、操作类型、操作参数、响应结果等信息。独立数据库文件存储，与业务库完全隔离。

## 一、审计日志表设计

### 1.1 独立数据库

使用与 `backup_meta.db` 相同的模式：独立 SQLite 文件 + 独立 `AuditBase` + 独立 engine/session。

- 文件路径：`{DATA_DIR}/audit.db`
- 与业务库 `tennis_diary.db` 完全隔离
- 备份/恢复操作不影响审计日志
- 模块加载时 `AuditBase.metadata.create_all()` 幂等建表，不引入 Alembic

### 1.2 表结构 `audit_logs`

| 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | Integer | PK, autoincrement | 主键 |
| `source` | String(16) | NOT NULL, index | `admin` / `user` |
| `admin_id` | Integer | nullable, index | 管理员 ID（source=admin 时） |
| `user_id` | Integer | nullable, index | 用户 ID（source=user 时） |
| `action` | String(32) | NOT NULL, index | `CREATE` / `UPDATE` / `DELETE` / `UPLOAD` / `ANALYZE` |
| `resource_type` | String(32) | NOT NULL, index | 资源类型 |
| `resource_id` | String(64) | nullable | 目标资源 ID |
| `description` | String(256) | default="" | 操作描述 |
| `request_body` | Text | nullable | 请求体 JSON（敏感字段脱敏） |
| `request_path` | String(256) | default="" | 请求路由路径（如 `/api/admin/admins`） |
| `request_method` | String(16) | default="" | HTTP 方法（POST/PUT/DELETE） |
| `response_code` | Integer | default=0 | HTTP 状态码（200/400/404/500） |
| `response_success` | Boolean | default=True | 操作是否成功 |
| `response_message` | String(256) | default="" | 响应 message（如"删除成功"） |
| `duration_ms` | Float | default=0 | 操作耗时（毫秒） |
| `ip_address` | String(45) | default="" | 客户端 IP（支持 IPv6） |
| `user_agent` | String(256) | default="" | User-Agent |
| `created_at` | String(32) | NOT NULL, index, 手动赋值 UTC Z 后缀 | 操作时间（`datetime.now(timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')`） |

> **不存冗余名称**：`admin_username` / `user_nickname` 不落库，查询时 JOIN 业务库 `admins` / `users` 表获取。

### 1.3 resource_type 映射

| 路径前缀 | resource_type | source |
|----------|---------------|--------|
| `/api/admin/admins` | `admin` | admin |
| `/api/admin/roles` | `role` | admin |
| `/api/admin/config` | `config` | admin |
| `/api/admin/config/providers` | `ai_provider` | admin |
| `/api/admin/diaries` | `diary` | admin |
| `/api/admin/gears` | `gear` | admin |
| `/api/admin/weights` | `weight` | admin |
| `/api/admin/checkins` | `checkin` | admin |
| `/api/admin/analyses` | `analysis` | admin |
| `/api/admin/posts` | `post` | admin |
| `/api/admin/users` | `user` | admin |
| `/api/admin/system` | `system` | admin |
| `/api/auth` | `user_profile` | user |
| `/api/auth/login` | `auth` | user |
| `/api/diaries` | `diary` | user |
| `/api/gears` | `gear` | user |
| `/api/weights` | `weight` | user |
| `/api/checkin` | `checkin` | user |
| `/api/analyses` | `analysis` | user |
| `/api/ai` | `ai` | user |
| `/api/video` | `video` | user |
| `/api/pose` | `pose` | user |
| `/api/upload` | `upload` | user |

### 1.4 action 映射

| HTTP 方法 | 默认 action | 说明 |
|-----------|-------------|------|
| POST | CREATE | 创建资源 |
| POST（AI/video/pose/upload） | ANALYZE / UPLOAD | 特殊操作类型 |
| PUT | UPDATE | 更新资源 |
| DELETE | DELETE | 删除资源 |

### 1.5 created_at 时区处理

`func.now()` 在 SQLite 中返回服务器本地时间，若服务器时区非东八区（如 Docker 默认 UTC），前端显示会产生偏移。修复方案：

| 字段 | 原方案 | 新方案 |
|------|--------|--------|
| `created_at` | `DateTime` + `server_default=func.now()` | `String(32)` + 手动赋值 `datetime.now(timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')` |

- 统一用 `Z` 后缀表示 UTC，与 Step 87（`system.py` 备份记录的 `strftime('...Z')`）一致
- 前端 `formatIso()` 用 `timeZone: 'Asia/Shanghai'` 转换，收到带 `Z` 的 ISO 字符串后自动转为东八区显示

## 二、全量端点清单（41 个）

### 2.1 Admin 端（25 个）

| # | 路由文件 | 端点 | action | resource_type |
|---|----------|------|--------|---------------|
| 1 | `admin/admins.py` | `POST /api/admin/admins` | CREATE | admin |
| 2 | `admin/admins.py` | `PUT /api/admin/admins/{id}` | UPDATE | admin |
| 3 | `admin/admins.py` | `PUT /api/admin/admins/{id}/password` | UPDATE | admin |
| 4 | `admin/admins.py` | `PUT /api/admin/admins/{id}/status` | UPDATE | admin |
| 5 | `admin/admins.py` | `DELETE /api/admin/admins/{id}` | DELETE | admin |
| 6 | `admin/roles.py` | `POST /api/admin/roles` | CREATE | role |
| 7 | `admin/roles.py` | `PUT /api/admin/roles/{id}` | UPDATE | role |
| 8 | `admin/roles.py` | `DELETE /api/admin/roles/{id}` | DELETE | role |
| 9 | `admin/config.py` | `PUT /api/admin/config/{key}` | UPDATE | config |
| 10 | `admin/config.py` | `DELETE /api/admin/config/{key}` | DELETE | config |
| 11 | `admin/config.py` | `POST /api/admin/config/reset` | UPDATE | config |
| 12 | `admin/ai_providers.py` | `POST /api/admin/config/providers` | CREATE | ai_provider |
| 13 | `admin/ai_providers.py` | `PUT /api/admin/config/providers/{id}` | UPDATE | ai_provider |
| 14 | `admin/ai_providers.py` | `DELETE /api/admin/config/providers/{id}` | DELETE | ai_provider |
| 15 | `admin/diaries.py` | `DELETE /api/admin/diaries/{id}` | DELETE | diary |
| 16 | `admin/gears.py` | `DELETE /api/admin/gears/{id}` | DELETE | gear |
| 17 | `admin/weights.py` | `DELETE /api/admin/weights/{id}` | DELETE | weight |
| 18 | `admin/checkins.py` | `DELETE /api/admin/checkins/{id}` | DELETE | checkin |
| 19 | `admin/analyses.py` | `DELETE /api/admin/analyses/{id}` | DELETE | analysis |
| 20 | `admin/posts.py` | `DELETE /api/admin/posts/{id}` | DELETE | post |
| 21 | `admin/users.py` | `DELETE /api/admin/users/{id}` | DELETE | user |
| 22 | `admin/system.py` | `POST /api/admin/system/backup` | CREATE | system |
| 23 | `admin/system.py` | `POST /api/admin/system/restore/{id}` | UPDATE | system |
| 24 | `admin/system.py` | `DELETE /api/admin/system/backup/{id}` | DELETE | system |
| 25 | `admin/system.py` | `POST /api/admin/system/backup/upload` | UPLOAD | system |

### 2.2 小程序端（16 个）

| # | 路由文件 | 端点 | action | resource_type |
|---|----------|------|--------|---------------|
| 26 | `auth.py` | `POST /api/auth/login` | LOGIN | auth |
| 27 | `auth.py` | `PUT /api/auth/me` | UPDATE | user_profile |
| 27 | `diaries.py` | `POST /api/diaries` | CREATE | diary |
| 28 | `diaries.py` | `PUT /api/diaries/{id}` | UPDATE | diary |
| 29 | `diaries.py` | `DELETE /api/diaries/{id}` | DELETE | diary |
| 30 | `gears.py` | `POST /api/gears` | CREATE | gear |
| 31 | `gears.py` | `PUT /api/gears/{id}` | UPDATE | gear |
| 32 | `gears.py` | `DELETE /api/gears/{id}` | DELETE | gear |
| 33 | `weights.py` | `POST /api/weights` | CREATE | weight |
| 34 | `weights.py` | `DELETE /api/weights/{id}` | DELETE | weight |
| 35 | `checkin.py` | `POST /api/checkin` | CREATE | checkin |
| 36 | `analyses.py` | `POST /api/analyses` | CREATE | analysis |
| 37 | `analyses.py` | `DELETE /api/analyses/{id}` | DELETE | analysis |
| 38 | `upload.py` | `POST /api/upload/avatar` | UPLOAD | upload |
| 39 | `video.py` | `POST /api/video/upload` | UPLOAD | video |
| 40 | `ai.py` | `POST /api/ai/analyze` | ANALYZE | ai |
| 41 | `ai.py` | `POST /api/ai/caption` | ANALYZE | ai |
| 42 | `pose.py` | `POST /api/pose/analyze` | ANALYZE | pose |

> **排除**：`POST /api/events`（客户端埋点公开接口）

## 三、实现方案（中间件 + 装饰器）

### 3.1 架构设计

采用**中间件 + 装饰器**混合方案：

- **中间件**（`AuditMiddleware`）：自动捕获通用信息（method、path、status_code、IP、UA、耗时、请求体缓存）
- **装饰器**（`@audit()`）：路由函数加装饰器声明审计元数据（action、resource_type、resource_id），装饰器自动从 `kwargs` 提取身份（admin/user）并设置到 `request.state`
- **路由函数**：零修改，无需 `try/finally`、无审计代码侵入

职责分工：

| 信息 | 负责方 |
|------|--------|
| HTTP method / path | 中间件自动 |
| IP / User-Agent | 中间件自动 |
| 请求体 | 中间件自动缓存（body 重放解决消费冲突） |
| 响应状态码 | 中间件自动 |
| 耗时 | 中间件自动 |
| 身份（admin/user） | 装饰器自动从 `kwargs` 提取 |
| action / resource_type | 装饰器参数声明 |
| resource_id | 装饰器自动从函数参数提取 |
| 异常捕获 | 装饰器捕获 HTTPException / Exception |

### 3.2 新建文件

| 文件 | 说明 |
|------|------|
| `server/app/core/audit_db.py` | 独立审计库 engine/session（仿 `backup_meta.py`） |
| `server/app/models/audit_log.py` | `AuditLog` 模型（使用 `AuditBase`） |
| `server/app/services/audit_service.py` | `log_action()` 写入函数 |
| `server/app/decorators/audit.py` | `@audit()` 审计装饰器 |
| `server/app/middleware/audit.py` | `AuditMiddleware` 审计中间件 |
| `server/app/routers/admin/audit_logs.py` | Admin 查询端点 |
| `server/tests/routers/admin/test_audit_logs.py` | 后端测试（13 个用例） |
| `admin/src/api/audit-logs.ts` | Admin 前端 API 层 |
| `admin/src/views/system/audit-logs.vue` | Admin 前端审计日志页 |

### 3.3 修改文件

| 文件 | 修改内容 |
|------|----------|
| `server/app/main.py` | 注册审计中间件 + 审计日志路由 |
| `server/app/schemas/admin.py` | 新增 `AuditLogResponse` |
| 25 个 admin 路由文件 | 每个写操作端点加 `@audit()` 装饰器 |
| 15 个 user 路由文件 | 每个写操作端点加 `@audit()` 装饰器（含 pose.py） |
| `admin/src/router/routes.ts` | 新增 `/system/audit-logs` 路由 |

### 3.4 独立数据库架构（仿 `backup_meta.py`）

```python
# server/app/core/audit_db.py

import os
from pathlib import Path

from sqlalchemy import create_engine
from sqlalchemy.orm import declarative_base, sessionmaker

from app.core.config import settings

AUDIT_DB_NAME = "audit.db"


def _audit_db_path() -> str:
    return os.path.join(settings.DATA_DIR, AUDIT_DB_NAME)


db_dir = Path(_audit_db_path()).resolve().parent
os.makedirs(db_dir, exist_ok=True)

audit_engine = create_engine(
    f"sqlite:///{_audit_db_path()}",
    connect_args={"check_same_thread": False},
)

AuditBase = declarative_base()

AuditSession = sessionmaker(autocommit=False, autoflush=False, bind=audit_engine)

from app.models.audit_log import AuditLog  # noqa: E402, F401

AuditBase.metadata.create_all(bind=audit_engine)
```

### 3.5 审计装饰器（v1.5.0 修复）

```python
# server/app/decorators/audit.py（v1.5.0 修复版）
import inspect
import time
from functools import wraps
from fastapi import HTTPException, Request


def audit(action: str, resource_type: str, resource_id_key: str | None = None):
    """审计装饰器

    用法：
        @router.post("")
        @audit(action="CREATE", resource_type="diary")
        def create_diary(body: ..., user=Depends(get_current_user), db=Depends(get_db)):
            ...

        @router.delete("/{diary_id}")
        @audit(action="DELETE", resource_type="diary", resource_id_key="diary_id")
        def delete_diary(diary_id: int, ...):
            ...
    """
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            request: Request | None = kwargs.pop("request", None)
            if request is None:
                for arg in args:
                    if isinstance(arg, Request):
                        request = arg
                        break

            if request is None:
                return func(*args, **kwargs)

            # 自动提取身份（从依赖注入的参数中）
            admin = kwargs.get("admin")
            user = kwargs.get("user")
            if admin:
                request.state.audit_admin = admin
            if user:
                request.state.audit_user = user

            # 设置元数据
            request.state.audit_action = action
            request.state.audit_resource_type = resource_type
            request.state.audit_start_time = time.time()

            # 提取 resource_id
            if resource_id_key and resource_id_key in kwargs:
                request.state.audit_resource_id = str(kwargs[resource_id_key])

            try:
                result = func(*args, **kwargs)
                request.state.audit_response_success = True
                return result
            except HTTPException as e:
                request.state.audit_response_code = e.status_code
                request.state.audit_response_message = str(e.detail)
                request.state.audit_response_success = False
                raise
            except Exception:
                request.state.audit_response_code = 500
                request.state.audit_response_message = "服务器内部错误"
                request.state.audit_response_success = False
                raise

        # --- 关键：动态注入 request 参数到 wrapper 签名 ---
        # FastAPI 读取 wrapper.__signature__，发现 Request 类型注解后自动注入 request 对象。
        # 原始路由函数不需要声明 request: Request，装饰器透明处理。
        sig = inspect.signature(func)
        if "request" not in sig.parameters:
            new_params = [
                inspect.Parameter(
                    "request",
                    inspect.Parameter.POSITIONAL_OR_KEYWORD,
                    annotation=Request,
                ),
                *sig.parameters.values(),
            ]
            wrapper.__signature__ = sig.replace(parameters=new_params)

        # --- 双保险：审计元数据存为函数属性，供中间件 fallback ---
        wrapper._audit_action = action
        wrapper._audit_resource_type = resource_type
        wrapper._audit_resource_id_key = resource_id_key

        return wrapper
    return decorator
```

> **v1.5.0 修复说明**：原版装饰器通过 `kwargs.get("request")` 获取 request 对象，但 FastAPI 不会自动把 `request` 注入到未声明 `request: Request` 参数的路由函数中，导致装饰器无法设置 `request.state`，审计日志缺少 action/resource_type/admin_id 等字段。修复方案：
> 1. 动态修改 `wrapper.__signature__`，向路由函数签名注入 `request: Request` 参数，FastAPI 据此自动注入 request 对象
> 2. `kwargs.pop("request", None)` 移除注入的 request，避免传给原始路由函数（它不需要此参数）
> 3. 中间件增加 fallback：从 `request.scope["endpoint"]` 的 `_audit_*` 函数属性读取元数据（双保险）

### 3.6 审计中间件（v1.5.0 增加 fallback）

```python
# server/app/middleware/audit.py（v1.5.0 增强版）
import json
import time
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response

from app.services.audit_service import log_action


class AuditMiddleware(BaseHTTPMiddleware):
    """自动审计所有写操作（POST/PUT/DELETE）

    读取顺序：
    1. request.state（装饰器直接写入，主路径）
    2. request.scope["endpoint"] 函数属性（装饰器 fallback，兜底）
    3. HTTP method 默认值（未装饰的路由，如 OPTIONS 预检）
    """

    async def dispatch(self, request: Request, call_next) -> Response:
        # 只审计写操作
        if request.method not in ("POST", "PUT", "DELETE"):
            return await call_next(request)

        # 缓存 body（解决消费冲突）
        body_bytes = await request.body()

        start = time.time()
        response = await call_next(request)
        duration_ms = (time.time() - start) * 1000

        # 解析请求体
        request_body = None
        if body_bytes:
            try:
                request_body = json.loads(body_bytes)
            except (json.JSONDecodeError, UnicodeDecodeError):
                pass

        # --- 优先从 request.state 读取（装饰器主路径）---
        action = getattr(request.state, "audit_action", None)
        resource_type = getattr(request.state, "audit_resource_type", None)
        resource_id = getattr(request.state, "audit_resource_id", None)
        admin = getattr(request.state, "audit_admin", None)
        user = getattr(request.state, "audit_user", None)

        # --- 兜底：从路由函数属性读取（装饰器 fallback）---
        if action is None:
            endpoint = request.scope.get("endpoint")
            if endpoint and hasattr(endpoint, "_audit_action"):
                action = getattr(endpoint, "_audit_action", None)
                resource_type = getattr(endpoint, "_audit_resource_type", None)
                resource_id_key = getattr(endpoint, "_audit_resource_id_key", None)
                if resource_id_key and resource_id is None:
                    path_params = request.scope.get("path_params", {})
                    resource_id = str(path_params.get(resource_id_key, ""))

        # 最终兜底：使用 HTTP method
        if action is None:
            action = request.method
        if resource_type is None:
            resource_type = "unknown"

        # 执行审计写入
        log_action(
            request=request,
            admin=admin,
            user=user,
            action=action,
            resource_type=resource_type,
            resource_id=resource_id,
            request_body=request_body,
            request_path=str(request.url.path),
            request_method=request.method,
            response_code=getattr(request.state, "audit_response_code", 0) or response.status_code,
            response_success=getattr(request.state, "audit_response_success", True),
            response_message=getattr(request.state, "audit_response_message", ""),
            duration_ms=duration_ms,
        )

        return response
```

### 3.7 路由函数中的使用模式

```python
from app.decorators.audit import audit

@router.post("")
@audit(action="CREATE", resource_type="diary")
def create_diary(
    body: DiaryCreateRequest,
    user=Depends(get_current_user),
    db=Depends(get_db),
):
    diary = Diary(...)
    db.add(diary)
    db.commit()
    return ApiResponse(data=DiaryResponse.model_validate(diary))

@router.delete("/{diary_id}")
@audit(action="DELETE", resource_type="diary", resource_id_key="diary_id")
def delete_diary(
    diary_id: int,
    admin=Depends(require_permission("diaries:delete")),
    db=Depends(get_db),
):
    diary = db.query(Diary).filter(Diary.id == diary_id).first()
    if not diary:
        raise HTTPException(404, "日记不存在")
    db.delete(diary)
    db.commit()
    return ApiResponse(message="删除成功")
```

**路由函数零审计代码**：只需加一个 `@audit()` 装饰器。

### 3.9 Admin 查询端点

`GET /api/admin/audit-logs`

| 参数 | 类型 | 说明 |
|------|------|------|
| `offset` | int | 分页偏移，默认 0 |
| `limit` | int | 每页条数，默认 20 |
| `source` | str | 筛选来源：`admin` / `user` |
| `admin_id` | int | 筛选管理员 |
| `user_id` | int | 筛选用户 |
| `action` | str | 筛选操作类型 |
| `resource_type` | str | 筛选资源类型 |
| `start_date` | str | 起始日期（ISO 格式） |
| `end_date` | str | 截止日期（ISO 格式） |

返回：`ApiResponse[PaginatedData[AuditLogResponse]]`

**JOIN 关联名称**（不冗余存储，从业务库查询）：

```python
# 审计库查记录
audit_session = AuditSession()
query = audit_session.query(AuditLog)
# ... 筛选条件 ...
total = query.count()
logs = query.order_by(AuditLog.created_at.desc()).offset(offset).limit(limit).all()

# 业务库查名称（批量 IN 查询，避免 N+1）
from app.core.database import SessionLocal
from app.models.admin import Admin as AdminModel
from app.models.user import User as UserModel

biz_db = SessionLocal()
try:
    admin_ids = {l.admin_id for l in logs if l.admin_id}
    user_ids = {l.user_id for l in logs if l.user_id}
    admin_map = {a.id: a.username for a in biz_db.query(AdminModel).filter(AdminModel.id.in_(admin_ids)).all()} if admin_ids else {}
    user_map = {u.id: u.nickname for u in biz_db.query(UserModel).filter(UserModel.id.in_(user_ids)).all()} if user_ids else {}
finally:
    biz_db.close()
```

权限：`require_permission("system:config")`

### 3.10 Response Model

```python
class AuditLogResponse(BaseModel):
    id: int
    source: str
    admin_id: int | None = None
    admin_username: str | None = None   # JOIN 得到
    user_id: int | None = None
    user_nickname: str | None = None    # JOIN 得到
    action: str
    resource_type: str
    resource_id: str | None = None
    description: str
    request_body: dict | None = None
    request_path: str
    request_method: str
    response_code: int
    response_success: bool
    response_message: str
    duration_ms: float
    ip_address: str
    user_agent: str
    created_at: datetime | None = None
```

### 3.11 Admin 前端页面

`admin/src/views/system/audit-logs.vue`，路由 `/system/audit-logs`，权限 `system:config`。

功能：
- 表格列：时间、来源、操作者、操作类型、资源类型、资源 ID、响应状态、IP、耗时
- 筛选栏：来源下拉、操作类型下拉、资源类型下拉、日期范围
- 分页：共享 `Pagination` 组件
- 请求体详情弹窗：点击查看脱敏后的 `request_body`

### 3.12 测试策略

测试文件：`server/tests/routers/admin/test_audit_logs.py`

> **说明**：审计服务使用模块级 `AuditSession`（无依赖注入），测试直接使用真实审计库。中间件在请求时自动写入审计日志，测试验证操作成功后通过查询端点验证日志记录。

测试用例（13 个）：

**录制验证（6 个）**：
1. `test_create_admin_generates_audit_log` — POST 创建管理员
2. `test_delete_admin_generates_audit_log` — DELETE 管理员
3. `test_toggle_status_generates_audit_log` — PUT 切换状态
4. `test_reset_password_generates_audit_log` — PUT 重置密码
5. `test_create_role_generates_audit_log` — POST 创建角色
6. `test_delete_role_generates_audit_log` — DELETE 角色

**查询验证（7 个）**：
7. `test_list_audit_logs` — 基础列表查询
8. `test_filter_by_action` — 按 action 筛选
9. `test_filter_by_resource_type` — 按 resource_type 筛选
10. `test_filter_by_source` — 按 source 筛选
11. `test_pagination` — 分页参数验证
12. `test_response_fields` — 响应字段完整性验证
13. `test_empty_result_when_no_match` — 无匹配结果返回空列表

## 四、实施步骤

1. ✅ 创建 `server/app/core/audit_db.py`（独立库 engine/session + 幂等建表）
2. ✅ 创建 `server/app/models/audit_log.py`（AuditLog 模型，继承 AuditBase；created_at 用 String(32) + 手动赋 UTC Z 后缀）
3. ✅ 在 `server/app/services/audit_service.py` 中实现 `log_action()`（写入 UTC Z 后缀时间戳）
4. ✅ 创建 `server/app/decorators/audit.py`（`@audit()` 装饰器，同步函数）
5. ✅ 创建 `server/app/middleware/audit.py`（`AuditMiddleware` 中间件）
6. ✅ 在 25 个 admin 路由文件中添加 `@audit()` 装饰器
7. ✅ 在 15 个 user 路由文件中添加 `@audit()` 装饰器（含 pose.py）
8. ✅ 创建 `server/app/routers/admin/audit_logs.py`（查询端点，JOIN 业务库获取名称）
9. ✅ 在 `server/app/schemas/admin.py` 中添加 `AuditLogResponse`（created_at 类型为 str）
10. ✅ 在 `server/app/main.py` 中注册审计中间件 + 审计日志路由
11. ✅ 创建 `admin/src/api/audit-logs.ts`
12. ✅ 创建 `admin/src/views/system/audit-logs.vue`
13. ✅ 在 `admin/src/router/routes.ts` 中添加路由
14. ✅ 创建 `server/tests/routers/admin/test_audit_logs.py`（13 个测试）
15. ✅ 运行 `ruff check` + `ruff format` + `pytest -q` 全部通过
16. ✅ v1.6.0：AuditLog 模型加 `request_path` + `request_method` 列，log_action/middleware/schema/前端同步更新
17. ✅ v1.6.1：查询端点补传 request_path/request_method + 审计详情弹窗响应信息始终显示并移至请求体下方
