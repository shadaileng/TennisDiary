> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 88 |
> | 文档版本 | v1.0.0 |
> | 文档状态 | 🏁 已完成 |
> | 最后更新 | 2026-08-15 |
> | 对应功能/内容 | 分享工坊技术评分卡片添加六边形雷达图 + 维度点评列表 |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-08-15 | v1.0.0 | 初版 |
> | 2026-08-15 | v1.0.0 | 实施完成：drawRadar + 模板重构 + build 通过 |
>
> **关联文档**：[75-6：Phase 5 分享工坊](./75-6-Phase5-分享工坊.md)、[83：姿态可视化与六边形雷达图](./83-姿态可视化与六边形雷达图.md)

# 88：分享工坊技术评分卡片六边形维度点评

## 一、需求背景

### 1.1 现状分析

当前分享工坊"技术评分"模板（`miniapp/src/utils/shareCanvas.ts` 第 261-302 行）的展示形式：

- **大评分球**：圆形 + 总分数字
- **六维进度条**：名称 + 水平进度条 + 分值

**问题**：
- 信息密度中等，缺乏直观的维度分布可视化
- 与分析页面（`report.vue`）的"📐 分维度点评"展示形式不一致
- 用户无法快速识别强项和弱项

### 1.2 目标效果

参考分析页面（`report.vue` 第 30-44 行）的"分维度点评"展示形式：

1. **六边形雷达图**：直观展示六维评分分布
2. **维度点评列表**：每个维度显示名称 + 分数 + 进度条 + 点评文字

---

## 二、技术调研

### 2.1 现有实现

| 组件/文件 | 实现方式 | 说明 |
|-----------|---------|------|
| `RadarChart.vue` | Vue 组件 + Canvas 2d | 六边形雷达图组件，用于报告详情页 |
| `shareCanvas.ts` | 全屏离屏 Canvas | 分享卡片绘制逻辑，使用原生 Canvas API |

### 2.2 技术约束

- `RadarChart.vue` 是 Vue 组件，无法直接在全屏离屏 Canvas 中使用
- 需要在 `shareCanvas.ts` 中重新实现一套 Canvas 绘制逻辑
- 需要保持与 `RadarChart.vue` 一致的视觉风格（配色、几何参数）

### 2.3 数据结构

```typescript
// 六维维度数据（来自后端 AI 评分）
interface DimensionScore {
  name: string      // 维度名，如"准备启动"
  score: number     // 分数 0-100
  comment: string   // 点评文字
}
```

---

## 三、实现方案

### 3.1 修改文件

- `miniapp/src/utils/shareCanvas.ts`

### 3.2 布局设计

**分享卡片画布尺寸**：1080 × 1350 px

**新布局方案**：

```
┌─────────────────────────────────────────┐
│  header: "{kind}技术评分"               │  Y: 0-480
│         "教练分析 · {date}"              │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐    │
│  │       六边形雷达图               │    │  Y: 480-780 (300px)
│  │    (居中，半径 ~110px)           │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  维度点评列表（6项）：                   │  Y: 780-1250
│  ┌─────────────────────────────────┐    │
│  │ 准备启动  85  ████████░░  85   │    │
│  │ 点评文字...                     │    │
│  ├─────────────────────────────────┤    │
│  │ 动力链    72  ██████░░░░  72   │    │
│  │ 点评文字...                     │    │
│  ├─────────────────────────────────┤    │
│  │ ...其他维度                     │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  footer                                 │  Y: 1250-1350
└─────────────────────────────────────────┘
```

### 3.3 代码实现

#### 3.3.1 添加雷达图绘制函数

在 `shareCanvas.ts` 文件顶部（常量定义后）添加 `drawRadar` 辅助函数：

```typescript
const GRID = "#E7E9DF";  // 网格线颜色（与 RadarChart.vue 一致）

/** 绘制六边形雷达图（Canvas 2d） */
function drawRadar(
  ctx: CanvasRenderingContext2D,
  cx: number,
  cy: number,
  R: number,
  data: { name: string; score: number }[],
) {
  const n = data.length;
  if (n < 3) return;

  const angle = (i: number) => -Math.PI / 2 + (2 * Math.PI * i) / n;
  const pt = (i: number, r: number) => ({
    x: cx + r * Math.cos(angle(i)),
    y: cy + r * Math.sin(angle(i)),
  });

  // 三档网格环（0.33 / 0.66 / 1）
  ctx.lineWidth = 1;
  for (const ratio of [0.33, 0.66, 1]) {
    ctx.beginPath();
    for (let i = 0; i < n; i++) {
      const p = pt(i, R * ratio);
      if (i === 0) ctx.moveTo(p.x, p.y);
      else ctx.lineTo(p.x, p.y);
    }
    ctx.closePath();
    ctx.strokeStyle = GRID;
    ctx.stroke();
  }

  // 辐射线
  for (let i = 0; i < n; i++) {
    const p = pt(i, R);
    ctx.beginPath();
    ctx.moveTo(cx, cy);
    ctx.lineTo(p.x, p.y);
    ctx.strokeStyle = GRID;
    ctx.stroke();
  }

  // 分值多边形
  ctx.beginPath();
  for (let i = 0; i < n; i++) {
    const v = Math.min(Number(data[i].score) || 0, 100) / 100;
    const p = pt(i, R * v);
    if (i === 0) ctx.moveTo(p.x, p.y);
    else ctx.lineTo(p.x, p.y);
  }
  ctx.closePath();
  ctx.fillStyle = LIME;
  ctx.globalAlpha = 0.35;
  ctx.fill();
  ctx.globalAlpha = 1;
  ctx.strokeStyle = LIME;
  ctx.lineWidth = 2;
  ctx.lineJoin = "round";
  ctx.stroke();

  // 顶点标签：名称 + 分值
  ctx.textAlign = "center";
  ctx.textBaseline = "middle";
  for (let i = 0; i < n; i++) {
    const lp = pt(i, R + 18);
    ctx.font = font(500, 26);
    ctx.fillStyle = GRAY;
    ctx.fillText(String(data[i].name), lp.x, lp.y - 8);
    ctx.font = font(700, 28);
    ctx.fillStyle = INK;
    ctx.fillText(String(Math.round(Number(data[i].score) || 0)), lp.x, lp.y + 10);
  }
}
```

#### 3.3.2 重构"技术评分"模板绘制逻辑

替换第 261-302 行为：

```typescript
// 技术评分
if (!latestAnalysis?.report) {
  header(ctx, "技术评分", "教练分析");
  emptyCard(ctx, "还没有分析报告，先去做一次分析吧～");
} else {
  const r = latestAnalysis.report;
  header(
    ctx,
    `${latestAnalysis.kind}技术评分`,
    `教练分析 · ${latestAnalysis.date}`,
  );

  const dims = (r.dimensions || []).slice(0, 6);

  if (dims.length >= 3) {
    // 有足够维度数据：显示雷达图 + 维度点评列表
    white(ctx, 70, 480, 940, 780);

    // 绘制雷达图（居中）
    const radarCx = W / 2;
    const radarCy = 630;
    const radarR = 110;
    drawRadar(ctx, radarCx, radarCy, radarR, dims);

    // 维度点评列表
    let sy = 780;
    for (const d of dims) {
      // 维度名称 + 分数
      ctx.font = font(600, 30);
      ctx.fillStyle = INK;
      ctx.textAlign = "left";
      ctx.fillText(d.name, 120, sy);

      ctx.font = font(700, 32);
      ctx.fillStyle = LIME;
      ctx.textAlign = "right";
      ctx.fillText(String(d.score ?? 0), 940, sy);

      // 进度条
      ctx.fillStyle = "#E8E8E4";
      rr(ctx, 120, sy + 14, 800, 12, 6);
      ctx.fill();
      ctx.fillStyle = LIME;
      rr(ctx, 120, sy + 14, 800 * Math.min(1, Number(d.score || 0) / 100), 12, 6);
      ctx.fill();

      // 点评文字（如果有）
      if (d.comment) {
        ctx.font = font(400, 24);
        ctx.fillStyle = "#7A8272";
        ctx.textAlign = "left";
        const commentLines = wrap(ctx, d.comment, 860);
        let cy = sy + 44;
        for (const line of commentLines.slice(0, 2)) {
          ctx.fillText(line, 120, cy);
          cy += 32;
        }
        sy = cy + 16;
      } else {
        sy += 60;
      }
    }
  } else {
    // 维度数据不足：显示大评分球
    white(ctx, 70, 480, 940, 580);
    ctx.fillStyle = LIME;
    ctx.beginPath();
    ctx.arc(W / 2, 700, 130, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = INK;
    ctx.font = font(800, 110);
    ctx.textAlign = "center";
    ctx.fillText(String(latestAnalysis.score || "—"), W / 2, 740);
    ctx.textAlign = "left";
  }

  // 底部：总结文字
  if (r.summary) {
    ctx.font = font(500, 26);
    ctx.fillStyle = "#7A8272";
    ctx.textAlign = "center";
    const lines = wrap(ctx, r.summary, 860);
    let sy = 1290;
    for (const line of lines.slice(0, 1)) {
      ctx.fillText(line, W / 2, sy);
      sy += 36;
    }
  }
}
```

---

## 四、关键参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `radarR` | 110 | 雷达图外接圆半径 |
| `GRID` | `#E7E9DF` | 网格线颜色（与 RadarChart.vue 一致） |
| `LIME` | `#C8DA2B` | 分值多边形填充色（与 RadarChart.vue 一致） |
| 顶点名称字号 | 26px | 适配卡片尺寸 |
| 顶点分值字号 | 28px | 适配卡片尺寸 |
| 维度点评区 | Y: 780-1250 | 约 470px 高度 |
| 每项点评行数 | 最多 2 行 | 防止溢出 |

---

## 五、视觉效果对比

| 修改前 | 修改后 |
|--------|--------|
| 大评分球 + 6行进度条 | 雷达图 + 进度条 + 点评文字 |
| 信息密度中等 | 信息密度高，与分析页一致 |
| 无法直观看出维度分布 | 雷达图一目了然 |

---

## 六、降级策略

当维度数据不足时（`dimensions.length < 3`）：
- 回退到大评分球展示
- 保持原有视觉风格

---

## 七、测试要点

1. **功能测试**：
   - 有完整六维数据时，显示雷达图 + 点评列表
   - 维度数据不足 3 个时，显示大评分球
   - 点评文字超过 2 行时，正确截断

2. **视觉测试**：
   - 雷达图配色与 `RadarChart.vue` 一致
   - 维度点评列表布局整齐
   - 卡片整体高度不超出画布

3. **兼容性测试**：
   - 微信小程序真机预览正常
   - Canvas 2d 渲染无异常

---

## 八、执行计划

| 步骤 | 内容 | 预计耗时 |
|------|------|---------|
| 1 | 在 `shareCanvas.ts` 添加 `drawRadar` 函数 | 10 min |
| 2 | 重构"技术评分"模板绘制逻辑 | 15 min |
| 3 | 功能测试 + 视觉调整 | 10 min |
| 4 | 提交代码 | 5 min |
