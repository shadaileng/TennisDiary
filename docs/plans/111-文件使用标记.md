> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 111 |
> | 文档版本 | v1.1.0 |
> | 文档状态 | 🏁 已完成 |
> | 最后更新 | 2026-08-25 |
> | 对应功能/内容 | 文件使用标记（基于数据库引用核查：使用中 / 可清除） |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-08-25 | v1.3.0 | 新增 POST /api/admin/files/cleanup-orphans 端点 + Admin 扫描弹窗「立即清理」按钮（Step 112）。 |
| 2026-08-25 | v1.2.0 | 实施完成：19 测试用例通过，ruff check/format 通过，admin build 通过。 |
| 2026-08-25 | v1.1.0 | 修正 §三 核查规则：删除 dataURL 保守分支，改为纯路径匹配；重写 §六 注意事项（无 File 记录不在标记范围，与孤儿扫描是两回事）。 |
| 2026-08-25 | v1.0.0 | 初版：计算 usage_status 与 usage_reason，Admin 列表/统计/扫描弹窗展示 |
>
> **关联文档**：[109 文件管理系统](./109-文件管理系统.md) · [110 文件扫描功能](./110-文件扫描功能.md)

# Step 111：文件使用标记（基于数据库引用核查）

## 一、目标

在现有 `File` 表 `ref_count` + `deleted_at` 基础上，新增**明确的「使用中 / 可清除」语义标记**，通过**实时核查业务表引用**计算，不引入新 DB 列（避免脏数据），并在后端 API 与管理端双向暴露。

核心问题：
1. 管理端当前只显示 `ref_count`，没有明确的使用状态标记；
2. `ref_count` 可能因业务删除时漏调 `decrement_ref_count` 而虚高——记录显示在用，实际业务已不引用；
3. 磁盘孤儿文件（未在 File 表注册）完全无状态标注。

目标效果：
- 管理端文件列表每行展示「使用中」或「可清除」彩色徽章，hover 显示原因；
- 统计卡片增加「可清除」计数；
- 扫描弹窗中的孤儿文件标注为「可清除·孤儿」；
- 可选按使用状态过滤。

## 二、状态定义

新增 `usage_status` 字段，取值与含义：

| 值 | 中文标签 | 判定条件 | 是否可安全清理 |
|----|---------|---------|:---:|
| `in_use` | 使用中 | 业务记录存在且仍引用该文件 | ✗ |
| `unreferenced` | 可清除（引用失效） | File 记录存在，但引用的业务记录不存在或不再引用 | ✓ |
| `marked_deleted` | 可清除（已软删） | `deleted_at` 非空，等待物理清理 | ✓ |
| `orphan` | 可清除（磁盘孤儿） | 文件在磁盘存在，但 File 表无记录 | ✓ |

`usage_reason` 提供具体原因字符串，用于 hover 提示，例如：
- `"引用计数已归零"`
- `"用户头像引用已失效"`
- `"装备记录已被删除"`
- `"分析报告记录不存在"`
- `"已标记删除，待物理清理"`
- `"磁盘孤儿，未注册到文件表"`

## 三、核查规则（`classify_file_usage`）

新建服务函数 `file_service.classify_file_usage(db, file_record: File) -> tuple[str, str]`，按优先级顺序判定。所有匹配均为**精确路径比对**（业务表存的就是与 File.rel_path 同源的字符串）。

1. **已软删**：`file_record.deleted_at is not None` → `("marked_deleted", "已标记删除，待物理清理")`
2. **引用归零**：`file_record.ref_count <= 0` → `("unreferenced", "引用计数已归零")`
3. **业务记录缺失**：`file_record.business_id is None` → `("unreferenced", "未绑定业务记录")`
4. **按 business_type 实时查业务表并做路径匹配**：
   - `user`：查 `User(id == business_id)`；若存在且 `avatar_url == rel_path` → `("in_use", "用户头像引用有效")`；否则 → `("unreferenced", "用户头像引用已失效")`
   - `gear`：查 `Gear(id == business_id)`；若存在且 `photo == rel_path` → `("in_use", "装备图片引用有效")`；否则 → `("unreferenced", "装备记录引用已失效")`
     - > **说明**：凡是有 `business_type=gear` 的 `File` 记录，当初必由 `/api/upload/gear-image` 生成，其 `photo` 字段即为 `rel_path`（不会是 dataURL）。若业务表 `photo` 与 `File.rel_path` 不一致，说明该 gear 已换图，原文件确实不再被引用 → 标「可清除」是正确的。无需额外保守处理。
   - `analysis`：查 `Analysis(id == business_id)`；若存在且 `(video_url or thumb or highlights or pose)` 任一含 `rel_path` → `("in_use", "分析报告引用有效")`；否则 → `("unreferenced", "分析报告引用已失效")`
   - 未知 `business_type` 且 `business_id is not None`：无法验证 → `("unreferenced", f"未知业务类型 {business_type}")`（保守不猜）

> **路径一致性验证**：业务表存储路径与 `File.rel_path` 同源，均通过 `file_service.make_rel_path(dir, user_id, filename)` 生成（`{dir}/{user_id}/{uuid}.{ext}`）。各上传端点（`upload_avatar`、`upload_gear_image`、`video/upload`）与落库流程（`register_ai_files`）都用同一返回值，确保精确匹配有效。

## 四、后端改动

### 4.1 Schema 变更：`app/schemas/admin_file.py`

- 新增枚举 `FileUsageStatus`（或直接复用 `Literal` 类型）；为清晰起见用枚举类。
- `AdminFileResponse` 增加：
  ```python
  usage_status: str = Field(description="使用状态：in_use/unreferenced/marked_deleted/orphan")
  usage_reason: str = Field(default="", description="使用状态原因说明")
  ```
- `OrphanFileInfo` 增加同样两个字段（孤儿固定 `orphan` + "磁盘孤儿，未注册到文件表"）。

### 4.2 服务层：`app/services/file_service.py`

- 新增 `classify_file_usage(db: Session, file_record: File) -> tuple[str, str]`（参见 §三）。
- 修改 `scan_orphan_files`：在构造的 orphans 字典中增加 `usage_status`、`usage_reason` 字段。
- 注意：`classify_file_usage` 中需要导入 `User`、`Gear`、`Analysis` 模型。

### 4.3 路由层：`app/routers/admin/files.py`

- `_file_to_response`：调用 `classify_file_usage`，把结果填入响应（列表接口每行一次查询，admin ≤100 行可接受）。
- `list_files`：新增可选查询参数 `usage_status: str | None = Query(None, description="按使用状态过滤")`；若提供则追加 `File.usage_status == usage_status` 过滤（需在 DB 上存该字段或改用内存过滤）。
  - **权衡**：由于 `usage_status` 是实时计算，不做存储，列表端**不支持 server-side 按 usage_status 过滤**；改为客户端 JS 过滤（Admin 页面本地过滤）。理由：N+1 已不可控，再加索引无意义。
- `file_stats`：新增 `marked_deleted_count` 与 `unreferenced_count`（廉价聚合查询）供统计卡片使用：
  ```python
  marked_deleted = db.query(File).filter(File.deleted_at.isnot(None)).count()
  unreferenced = db.query(File).filter(File.ref_count <= 0, File.deleted_at.is_(None)).count()
  ```
  并返回给管理端。

### 4.4 测试：`tests/routers/admin/test_files_usage.py`

覆盖用例：
- `test_classify_marked_deleted`：deleted_at 非空 → marked_deleted
- `test_classify_ref_zero`：ref_count=0 → unreferenced
- `test_classify_no_business_id`：business_id 为 None → unreferenced
- `test_classify_user_in_use`：User.avatar_url 精确等于 rel_path → in_use
- `test_classify_user_unreferenced`：User 记录不存在 / avatar_url 不匹配 → unreferenced
- `test_classify_gear_in_use`：Gear.photo 精确等于 rel_path → in_use
- `test_classify_gear_unreferenced`：Gear 记录不存在 / photo 不匹配 → unreferenced
- `test_classify_analysis_in_use`：Analysis.video_url 或 thumb 包含 rel_path → in_use
- `test_classify_analysis_unreferenced`：Analysis 记录不存在 / 无任何字段含 rel_path → unreferenced
- `test_classify_unknown_business_type`：已知 business_id 但 business_type 不在 {user,gear,analysis} → unreferenced（保守不猜）
- `test_scan_orphan_with_usage_status`：扫描结果含 usage_status="orphan" 与对应 reason

### 4.5 验证命令

```bash
cd server && uv run ruff check . && uv run ruff format . && uv run pytest -q tests/routers/admin/test_files_usage.py
```

## 五、管理端改动

### 5.1 API 客户端：`admin/src/api/files.ts`

- `AdminFile` 接口增加：
  ```typescript
  usage_status: string
  usage_reason: string
  ```
- `OrphanFileInfo` 接口增加同样两个字段。
- 保持 `getFiles` 签名不变（服务端不做 usage_status 过滤，客户端过滤）。

### 5.2 文件管理页：`admin/src/views/files/index.vue`

#### 5.2.1 工具栏
- 新增「使用状态」下拉筛选（客户端过滤）：
  ```vue
  <select v-model="filterUsageStatus">
    <option value="">全部状态</option>
    <option value="in_use">使用中</option>
    <option value="unreferenced">可清除·引用失效</option>
    <option value="marked_deleted">可清除·已软删</option>
    <option value="orphan">可清除·孤儿</option>
  </select>
  ```
- 点击筛选时触发 `fetchFiles`（当前页重置为 1）。

#### 5.2.2 列表列
- 新增「使用状态」列，含彩色徽章组件逻辑：
  ```ts
  const usageBadge = (status: string) => {
    const map = {
      in_use: { label: '使用中', class: 'bg-green-100 text-green-700' },
      unreferenced: { label: '可清除', class: 'bg-orange-100 text-orange-700' },
      marked_deleted: { label: '可清除', class: 'bg-gray-200 text-gray-600' },
      orphan: { label: '可清除·孤儿', class: 'bg-red-100 text-red-700' },
    }
    return map[status] || { label: status, class: 'bg-gray-100' }
  }
  ```
  单元格内容带 `:title="row.usage_reason"` 以 hover 显示原因。

#### 5.2.3 详情弹窗
- 新增「使用状态」与「原因」两行，沿用同样的徽章样式。

#### 5.2.4 统计卡片
- 在现有 4 卡片基础上，新增「可清除」卡片，数值来自 stats 返回的 `marked_deleted_count + unreferenced_count`。

#### 5.2.5 扫描弹窗孤儿行
- 孤儿表格新增「状态」列，显示「可清除·孤儿」徽章。

#### 5.2.6 客户端过滤
- 因服务端不支持按 usage_status 过滤，在 `fetchFiles` 后对 `res.items` 做 `Array.prototype.filter` 客户端过滤，并同步更新 `total`（仅当过滤后非空时；否则显示原始 total 并加「显示过滤结果」提示）。

### 5.3 验证命令

```bash
cd admin && pnpm run type-check && pnpm run build
```

## 六、注意事项

1. **无数据库迁移**：所有状态实时计算，不新增列，无需 Alembic 迁移。
2. **标记范围仅限 File 表已有行**：`usage_status` 仅对已进入 `File` 表的记录计算。未注册的磁盘孤儿通过「扫描」功能单独处理（已用 `orphan` 状态标注），两者互不干扰；不是「未覆盖的异常」，是「不在本功能的标记对象内」。
3. **性能**：每文件一次 classify 查询（≤3 条 SELECT），admin 分页 ≤100 行 → 最多 300 条查询。对本地 SQLite 可接受；若后续数据量大，可优化为批量查询 + 内存 map。
4. **一致性**：业务表写入/删除时不缓存 usage_status，每次列表刷新即重算，保证数据一致。
5. **AGENTS.md 规则 4（共享响应模型变更）**：`AdminFileResponse` 已在 `app/schemas/admin_file.py` 中全量使用（grep 确认只有 `admin/files.py::_file_to_response` 一处构造，无 model_validate 散点）；新增字段只需改一处。`OrphanFileInfo` 同理，只在 `scan_orphan_files` 结果映射处。
6. **异常日志风格**：`file_service.py` 新增代码需遵循 AGENTS.md §6.1：禁止 `f"...{exc}"`，使用 `%s` 风格。
7. **路径匹配假设已验证**：各上传端点（`upload_avatar`、`upload_gear_image`、`video/upload`）返回的 `url` 即为 `File.rel_path`，客户端回写业务表时原样存储；`register_ai_files` 亦用同一批 rel_path 注册。精确路径比对可靠。

## 七、交付清单

- [ ] `app/schemas/admin_file.py`：新增 `usage_status`/`usage_reason` 字段、`FileUsageStatus` 常量
- [ ] `app/services/file_service.py`：实现 `classify_file_usage`（纯路径匹配，无 dataURL 分支）、更新 `scan_orphan_files`
- [ ] `app/routers/admin/files.py`：接入 classify、更新 stats、加查询参数占位（实际走客户端过滤）
- [ ] `tests/routers/admin/test_files_usage.py`：11 用例覆盖（含未知 business_type 与无 business_id 场景）
- [ ] `admin/src/api/files.ts`：类型同步
- [ ] `admin/src/views/files/index.vue`：UI 徽章、筛选、详情、统计卡片、扫描弹窗
- [ ] 后端 `ruff check + format + pytest` 通过
- [ ] 前端 `type-check + build` 通过
- [ ] 方案文档状态更新为 ✅ 已完成

## 八、关联迭代：Step 112 — 立即清理孤儿文件

在 Step 111 的基础上，Admin 扫描弹窗新增「立即清理」按钮，支持物理删除孤儿文件。

### 实现要点

1. **后端**
   - `file_service.cleanup_orphan_paths(rel_paths: list[str]) -> int`：物理删除指定路径文件，返回成功数
   - `POST /api/admin/files/cleanup-orphans`：接受 `CleanupOrphansRequest{files}`，带审计日志
   - `app/schemas/admin_file.py`：新增 `CleanupOrphansRequest`

2. **前端**
   - `admin/src/api/files.ts`：新增 `cleanupOrphanFiles(files: string[])`
   - `admin/src/views/files/index.vue`：扫描弹窗孤儿操作栏新增红色「立即清理」按钮，与「纳入管理」并列；确认后调 API，清理后刷新列表和统计

3. **测试**
   - `tests/routers/admin/test_cleanup_orphans.py`：3 个用例（正常清理、空列表、跳过不存在文件）

4. **验证**
   - 后端：ruff check/format 通过，3 个新测试 + 原有 45 个 admin 测试通过
   - 前端：admin build 通过
