> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 59 |
> | 文档版本 | v1.0.0 |
> | 文档状态 | ✅ 已完成 |
> | 最后更新 | 2026-08-08 |
> | 对应功能/内容 | 小程序事件锚点 + 后台事件日志接口 |
> | 关联文档 | [B2 后台管理API总纲](./43-B2-后台管理API总纲.md)、[系统监控API](./46-B2-3-系统监控API.md) |

# Step 59：小程序事件锚点与线上事件日志

## 一、目标

为小程序添加事件锚点（event logger），记录运行期各层级信息（网络异常、业务异常、全局崩溃、自定义事件），并通过后台 API 持久化存储，供管理端查询。

**设计原则：**
- 错误/严重级别（error/fatal）实时上报
- 普通事件（info/warn）批量攒批上报（≤3s 或 ≥5条）
- 上报失败不阻塞业务，仅本地缓存兜底
- 后台管理端新增「事件日志」查询入口

## 二、前置条件

- Phase B2 已完成（管理员认证、系统监控 API）
- 小程序登录态已接入（`useAuthStore`）
- 现有网络层（`services/request.ts`）已封装

## 三、方案总览

```
┌──────────────────────────────────────────────────────────────┐
│  小程序端（miniapp）                                          │
│                                                              │
│  utils/eventLogger.ts  ← 统一入口                             │
│    ├── info / warn / error / fatal                          │
│    ├── 实时上报（error/fatal，立即 uni.request）               │
│    ├── 批量上报（info/warn，≤3s 或 ≥5条，防抖）                │
│    └── 本地缓存（StorageSync，启动时补发离线事件）              │
│                                                              │
│  埋点位置：                                                    │
│  ├── request.ts → 网络请求异常（fail + 业务错误）              │
│  ├── App.vue → onError（全局崩溃）                            │
│  └── 业务代码 → 主动调用 eventLogger.error/warn(...)          │
└──────────────────────────────────────────────────────────────┘
                          ↓ HTTP POST
┌──────────────────────────────────────────────────────────────┐
│  后台服务（server）                                            │
│                                                              │
│  models/event_log.py         ← 新表 event_logs                │
│  schemas/schemas.py          ← EventLogCreate/Response        │
│  routers/events.py           ← POST /api/events（公开，免鉴权）│
│  routers/admin/events.py     ← GET /api/admin/events（需鉴权）│
│                                                              │
│  表字段：                                                     │
│  id, user_id(外键,可为空), level, type, message,            │
│  stack, page, extra(JSON), device_info(JSON),                │
│  created_at                                                 │
└──────────────────────────────────────────────────────────────┘
                          ↓ 查询
┌──────────────────────────────────────────────────────────────┐
│  后台管理端（admin）                                          │
│                                                              │
│  系统监控页新增「事件日志」标签页                               │
│  - 按级别过滤（info/warn/error/fatal）                        │
│  - 按用户搜索                                                 │
│  - 分页列表展示                                               │
└──────────────────────────────────────────────────────────────┘
```

## 四、详细执行步骤

### 4.1 数据库模型

**文件**：`server/app/models/event_log.py`（新增）

```python
from sqlalchemy import Column, DateTime, ForeignKey, Integer, String, Text, func

from app.core.database import Base


class EventLog(Base):
    __tablename__ = "event_logs"

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=True, index=True)
    level = Column(String(16), nullable=False, index=True)  # info/warn/error/fatal
    type = Column(String(64), nullable=False, default="custom")  # network/business/crash/custom
    message = Column(Text, nullable=False)
    stack = Column(Text, default="")
    page = Column(String(256), default="")  # 页面路径
    extra = Column(Text, default="")  # JSON 扩展字段
    device_info = Column(Text, default="")  # 设备信息 JSON
    created_at = Column(DateTime, server_default=func.now(), index=True)
```

**文件**：`server/app/models/__init__.py`（追加导入）

```python
from app.models.event_log import EventLog
```

### 4.2 Alembic 迁移

```bash
cd server
uv run alembic revision --autogenerate -m "add event_logs table"
uv run alembic upgrade head
```

### 4.3 Schema 定义

**文件**：`server/app/schemas/schemas.py`（追加）

```python
# ==================== 事件日志 ====================

class EventLogCreate(BaseModel):
    level: str = Field(..., pattern="^(info|warn|error|fatal)$")
    type: str = "custom"
    message: str
    stack: str = ""
    page: str = ""
    extra: dict = {}
    device_info: dict = {}


class EventLogResponse(EventLogCreate):
    id: int
    user_id: int | None
    created_at: float

    model_config = {"from_attributes": True}
```

### 4.4 事件日志路由（用户端，公开免鉴权）

**文件**：`server/app/routers/events.py`（新增）

```python
"""小程序事件日志上报路由（公开接口，免鉴权）"""

from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.core.logging import get_logger
from app.core.database import get_db
from app.models.event_log import EventLog
from app.schemas.common import ApiResponse
from app.schemas.schemas import EventLogCreate, EventLogResponse

log = get_logger("user")

router = APIRouter(prefix="/api/events", tags=["events"])


@router.post("", response_model=ApiResponse[EventLogResponse])
async def create_event_log(body: EventLogCreate, db: Session = Depends(get_db)):
    """上报事件日志（小程序端调用，无需鉴权）"""
    user_id = body.extra.get("user_id")
    if user_id:
        try:
            user_id = int(user_id)
        except (ValueError, TypeError):
            user_id = None

    event = EventLog(
        user_id=user_id,
        level=body.level,
        type=body.type,
        message=body.message,
        stack=body.stack,
        page=body.page,
        extra=body.extra,
        device_info=body.device_info,
    )
    db.add(event)
    db.commit()
    db.refresh(event)

    log.info(f"事件日志上报: {body.level} {body.type}", event_id=event.id)
    return ApiResponse(data=EventLogResponse.model_validate(event))
```

### 4.5 事件日志路由（管理端，需鉴权）

**文件**：`server/app/routers/admin/events.py`（新增）

```python
"""管理端事件日志查询路由"""

from fastapi import APIRouter, Depends, Query
from sqlalchemy.orm import Session

from app.core.auth import get_current_admin
from app.core.database import get_db
from app.models.admin import Admin
from app.models.event_log import EventLog
from app.schemas.common import ApiResponse, PaginatedData
from app.schemas.schemas import EventLogResponse

router = APIRouter(prefix="/api/admin/events", tags=["admin-events"])


@router.get("", response_model=ApiResponse[PaginatedData[EventLogResponse]])
def list_event_logs(
    level: str | None = Query(None, description="按级别过滤：info/warn/error/fatal"),
    type: str | None = Query(None, description="按类型过滤：network/business/crash/custom"),
    user_id: int | None = Query(None, description="按用户ID过滤"),
    keyword: str | None = Query(None, description="关键字搜索 message"),
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    admin: Admin = Depends(get_current_admin),
    db: Session = Depends(get_db),
):
    """查询事件日志（分页，支持多条件过滤）"""
    query = db.query(EventLog)

    if level:
        query = query.filter(EventLog.level == level)
    if type:
        query = query.filter(EventLog.type == type)
    if user_id:
        query = query.filter(EventLog.user_id == user_id)
    if keyword:
        query = query.filter(EventLog.message.like(f"%{keyword}%"))

    total = query.count()
    items = (
        query
        .order_by(EventLog.created_at.desc())
        .offset((page - 1) * page_size)
        .limit(page_size)
        .all()
    )

    return ApiResponse(
        data=PaginatedData(
            items=[EventLogResponse.model_validate(e) for e in items],
            total=total,
            offset=(page - 1) * page_size,
            limit=page_size,
        )
    )
```

### 4.6 注册路由

**文件**：`server/app/main.py`（追加导入和注册）

```python
from app.routers import events as user_events
from app.routers.admin import events as admin_events

# 在现有路由注册区域追加：
app.include_router(user_events.router)
app.include_router(admin_events.router)
```

### 4.7 小程序端 EventLogger

**文件**：`miniapp/src/utils/eventLogger.ts`（新增）

```typescript
/**
 * 事件日志上报工具
 *
 * 四级：info / warn / error / fatal
 * 上报策略：
 * - error/fatal：实时上报（立即发送）
 * - info/warn：批量上报（防抖 3s 或 ≥5 条时触发）
 * - 上报失败不阻塞业务，本地缓存兜底，下次启动补发
 */

import { API_PREFIX, BASE_URL } from "@/config";
import { STORAGE_KEYS } from "@/constants/storage";
import { useAuthStore } from "@/stores/auth";

// ==================== 类型 ====================

export type LogLevel = "info" | "warn" | "error" | "fatal";
export type EventType = "network" | "business" | "crash" | "custom";

export interface EventLogPayload {
  level: LogLevel;
  type: EventType;
  message: string;
  stack?: string;
  page?: string;
  extra?: Record<string, any>;
  deviceInfo?: Record<string, any>;
}

// ==================== 常量 ====================

const BATCH_FLUSH_INTERVAL_MS = 3000;
const BATCH_THRESHOLD = 5;
const MAX_PENDING_CACHE = 50;
const STORAGE_KEY_PENDING = STORAGE_KEYS.eventLogPending;

// ==================== 状态 ====================

let pendingBatch: EventLogPayload[] = [];
let flushTimer: ReturnType<typeof setTimeout> | null = null;

// ==================== 工具 ====================

/** 获取设备信息（仅首次获取，后续缓存） */
function getDeviceInfo(): Record<string, any> {
  try {
    const info = uni.getSystemInfoSync();
    return {
      platform: info.platform,
      model: info.model,
      system: info.system,
      screenWidth: info.screenWidth,
      screenHeight: info.screenHeight,
      brand: info.brand,
    };
  } catch {
    return {};
  }
}

/** 获取当前页面路径 */
function getCurrentPage(): string {
  try {
    const pages = getCurrentPages();
    const page = pages[pages.length - 1];
    return page ? page.route : "";
  } catch {
    return "";
  }
}

/** 上报单条事件日志 */
function flushOne(payload: EventLogPayload): void {
  const authStore = useAuthStore();
  const token = uni.getStorageSync(STORAGE_KEYS.token) as string;

  const body: Record<string, any> = {
    level: payload.level,
    type: payload.type,
    message: payload.message,
    stack: payload.stack || "",
    page: payload.page || getCurrentPage(),
    extra: payload.extra || {},
    device_info: payload.deviceInfo || getDeviceInfo(),
  };

  if (authStore.isLoggedIn && authStore.user) {
    body.extra.user_id = authStore.user.id;
  }

  const fullUrl = `${BASE_URL}${API_PREFIX}/events`;
  uni.request({
    url: fullUrl,
    method: "POST",
    data: body,
    header: {
      "Content-Type": "application/json",
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
    },
    timeout: 5000,
    success() { /* 静默成功 */ },
    fail() {
      /* 上报失败：加入本地缓存，下次启动补发 */
      appendToPending(payload);
    },
  });
}

/** 追加到本地缓存（启动时补发） */
function appendToPending(payload: EventLogPayload): void {
  try {
    const raw = uni.getStorageSync(STORAGE_KEY_PENDING) as string;
    const cached: EventLogPayload[] = raw ? JSON.parse(raw) : [];
    cached.push(payload);
    if (cached.length > MAX_PENDING_CACHE) cached.splice(0, cached.length - MAX_PENDING_CACHE);
    uni.setStorageSync(STORAGE_KEY_PENDING, JSON.stringify(cached));
  } catch {
    /* 忽略缓存失败 */
  }
}

/** 补发本地缓存的离线事件 */
export function flushPendingEvents(): void {
  try {
    const raw = uni.getStorageSync(STORAGE_KEY_PENDING) as string;
    if (!raw) return;
    const cached: EventLogPayload[] = JSON.parse(raw);
    if (!cached.length) return;
    uni.removeStorageSync(STORAGE_KEY_PENDING);
    cached.forEach((p) => flushOne(p));
  } catch {
    /* 忽略 */
  }
}

/** 批量防抖 flush（info/warn 使用） */
function batchFlush(): void {
  if (flushTimer) clearTimeout(flushTimer);
  flushTimer = setTimeout(() => {
    flushTimer = null;
    const batch = [...pendingBatch];
    pendingBatch = [];
    batch.forEach(flushOne);
  }, BATCH_FLUSH_INTERVAL_MS);
}

// ==================== 公开 API ====================

/** 记录信息事件（批量上报） */
export function logInfo(message: string, extra?: Record<string, any>): void {
  const payload: EventLogPayload = {
    level: "info",
    type: "custom",
    message,
    extra,
  };
  pendingBatch.push(payload);
  batchFlush();
}

/** 记录警告事件（批量上报） */
export function logWarn(message: string, extra?: Record<string, any>): void {
  const payload: EventLogPayload = {
    level: "warn",
    type: "custom",
    message,
    extra,
  };
  pendingBatch.push(payload);
  if (pendingBatch.length >= BATCH_THRESHOLD) {
    batchFlush();
    pendingBatch = [];
  } else {
    batchFlush();
  }
}

/** 记录错误事件（实时上报） */
export function logError(message: string, extra?: Record<string, any>, stack?: string): void {
  flushOne({
    level: "error",
    type: "business",
    message,
    stack: stack || "",
    extra,
  });
}

/** 记录致命事件（实时上报） */
export function logFatal(message: string, extra?: Record<string, any>, stack?: string): void {
  flushOne({
    level: "fatal",
    type: "crash",
    message,
    stack: stack || "",
    extra,
  });
}
```

### 4.8 埋点：request.ts 网络异常

**文件**：`miniapp/src/services/request.ts`（修改）

在文件顶部追加导入：
```typescript
import { logError, logWarn } from "@/utils/eventLogger";
```

在 `fail` 回调中（约第 165 行）追加：
```typescript
fail: (err) => {
  logError(`网络请求失败 ${method} ${url}: ${err.errMsg || "未知错误"}`, {
    method, url, status: -1,
  });
  reject(new ApiError(-1, err.errMsg || "网络请求失败"));
},
```

在业务错误 reject 前（约第 150 行）追加：
```typescript
logError(`API业务错误 ${method} ${url}: ${apiRes.message}`, {
  method, url, statusCode, code: apiRes.code,
});
reject(new ApiError(statusCode, apiRes.message || "请求失败"));
```

### 4.9 埋点：App.vue 全局崩溃

**文件**：`miniapp/src/App.vue`（修改）

```vue
<script setup lang="ts">
import { onLaunch, onShow, onHide, onError } from "@dcloudio/uni-app";
import { useAuthStore } from "@/stores/auth";
import { useSettingsStore } from "@/stores/settings";
import { logFatal, flushPendingEvents } from "@/utils/eventLogger";

onLaunch(() => {
  const auth = useAuthStore();
  auth.init();
  useSettingsStore().init();
  // 启动时补发离线事件
  flushPendingEvents();
  console.log("App Launch");
});

onShow(() => { console.log("App Show"); });
onHide(() => { console.log("App Hide"); });

// 全局错误捕获
onError((err: any) => {
  const msg = typeof err === "string" ? err : (err?.message || JSON.stringify(err));
  logFatal("全局未捕获错误", { errMsg: msg });
});
</script>
```

### 4.10 后台管理端：事件日志页面

在现有系统监控页面（或新建独立路由）增加「事件日志」标签页，调用 `GET /api/admin/events`，支持：
- 级别下拉过滤（全部/info/warn/error/fatal）
- 类型下拉过滤（全部/network/business/crash/custom）
- 用户ID搜索框
- 关键字搜索
- 分页列表（显示：时间、级别、类型、用户、消息、页面）

参考现有 `Admin-6-系统监控.md`（计划文档53）的实现风格。

## 五、目录结构变更

```
server/
├── app/
│   ├── models/
│   │   └── event_log.py           # 新增
│   ├── routers/
│   │   ├── events.py              # 新增（公开，免鉴权）
│   │   └── admin/
│   │       └── events.py          # 新增（需鉴权）
│   └── schemas/
│       └── schemas.py             # 追加 EventLogCreate/Response
├── alembic/versions/              # 新增迁移脚本
└── pyproject.toml                 # 无变更

miniapp/
└── src/
    ├── utils/
    │   └── eventLogger.ts         # 新增
    ├── services/
    │   └── request.ts             # 追加埋点
    └── App.vue                    # 追加全局错误 + flushPendingEvents
```

## 六、API 契约

### 6.1 事件上报（公开，免鉴权）

```
POST /api/events
Content-Type: application/json

Request Body:
{
  "level": "error",       // info|warn|error|fatal
  "type": "network",      // network|business|crash|custom
  "message": "请求超时",
  "stack": "",
  "page": "pages/diary/index",
  "extra": { "user_id": 123, "url": "/api/diaries" },
  "device_info": { "platform": "ios", "model": "iPhone 15" }
}

Response:
{
  "code": 0,
  "message": "ok",
  "success": true,
  "data": {
    "id": 1,
    "user_id": 123,
    "level": "error",
    "type": "network",
    "message": "请求超时",
    ...
    "created_at": 1723123456.0
  }
}
```

### 6.2 事件日志查询（管理端，需 JWT admin）

```
GET /api/admin/events?level=error&type=network&user_id=123&keyword=超时&page=1&page_size=20

Response:
{
  "code": 0,
  "data": {
    "items": [...],
    "total": 150,
    "offset": 0,
    "limit": 20
  }
}
```

## 七、测试用例

### 后端（pytest）

| 测试 | 验证点 |
|------|--------|
| test_create_event_log | 普通事件创建成功，返回完整响应 |
| test_create_event_log_no_user | 未登录用户上报，user_id 为 null |
| test_list_event_logs_all | 查询全部事件，分页正确 |
| test_list_event_logs_filtered | 按 level/type/user_id/keyword 过滤正确 |
| test_list_event_logs_auth | 未鉴权返回 401 |

### 前端（手动验证）

| 场景 | 验证点 |
|------|--------|
| 网络请求失败 | 后台查到 error 级别 network 类型记录 |
| 全局崩溃 | onError 触发 fatal 级别 crash 类型记录 |
| 上报失败兜底 | 断网后事件存入 Pending，恢复后补发 |
| 批量上报 | 连续 5 次 info 触发一次批量 flush |
| 管理端查询 | 可在管理端页面按条件筛选查看 |

## 八、验收标准

| 验收项 | 标准 |
|--------|------|
| 四级日志级别 | info/warn/error/fatal 均能上报并存储 |
| 四种事件类型 | network/business/crash/custom 均能区分 |
| 错误实时上报 | error/fatal 立即发出，不等待批量 |
| 批量上报 | info/warn 防抖 3s 或 ≥5 条触发 |
| 离线缓存 | 上报失败时本地缓存，启动时补发 |
| 网络埋点 | request.ts fail 分支记录网络异常 |
| 全局崩溃 | App.vue onError 捕获并上报 |
| 管理端查询 | GET /api/admin/events 支持过滤分页 |
| 免鉴权上报 | POST /api/events 无需 token 可上报 |

## 九、提交规范

```bash
# 后端
git add server/app/models/event_log.py \
       server/app/routers/events.py \
       server/app/routers/admin/events.py \
       server/app/schemas/schemas.py \
       server/app/main.py \
       server/alembic/versions/*.py
git commit -m "feat(server): 新增事件日志模型、Schema 与管理端查询接口

- 新增 EventLog 数据模型（event_logs 表）
- 新增 /api/events 公开上报接口（免鉴权）
- 新增 /api/admin/events 管理端查询接口（支持过滤/分页）
- 更新 models/__init__.py 导出 EventLog"

# 前端
git add miniapp/src/utils/eventLogger.ts \
       miniapp/src/services/request.ts \
       miniapp/src/App.vue
git commit -m "feat(miniapp): 新增事件日志上报工具与埋点

- 新增 utils/eventLogger.ts（四级日志，实时/批量混合上报）
- request.ts 网络异常埋点（logError）
- App.vue 全局崩溃捕获（onError → logFatal）
- 启动时补发离线缓存事件（flushPendingEvents）"
```
